# MySQL (Hive Metastore)
# ------------------------------------------------
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=hive_metastore
MYSQL_USER=hive
MYSQL_PASSWORD=AWSh2h5jda953jA*aws
MYSQL_HOST=hive-mysql.c9g8s2io6i5d.eu-north-1.rds.amazonaws.com

# ------------------------------------------------
# Hive
# ------------------------------------------------
SERVICE_NAME=metastore

# ------------------------------------------------
# Spark cluster
# ------------------------------------------------
SPARK_IMAGE=nyc-taxi-data-pipeline-spark-master
SPARK_MASTER=spark://spark-master:7077

SPARK_SQL_HIVE_EXEC_DYNAMIC_PARTITION=true
SPARK_SQL_HIVE_EXEC_DYNAMIC_PARTITION_MODE=nonstrict

SPARK_WORKER_CORES=8
SPARK_WORKER_MEMORY=6g

# ------------------------------------------------
# Docker (Airflow DockerOperator)
# ------------------------------------------------
DOCKER_NETWORK=nyc-taxi-data-pipeline
HOST_PROJECT_PATH=/home/ubuntu/nyc-taxi-data-pipeline

# ------------------------------------------------
# Python runtime
# ------------------------------------------------
PYTHONPATH=/app
PYTHONUNBUFFERED=1

# ------------------------------------------------
# dbt
# ------------------------------------------------
DBT_USER=evgeni

# ------------------------------------------------
# Airflow Postgres
# ------------------------------------------------
POSTGRES_USER=posgres
POSTGRES_PASSWORD=AWSh2h5jda953jA*aws
POSTGRES_DB=airflow
POSTGRES_HOST=airflow-postgresql.c9g8s2io6i5d.eu-north-1.rds.amazonaws.com

# ------------------------------------------------
# Airflow
# ------------------------------------------------
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://postgres:AWSh2h5jda953jA*aws@airflow-postgresql.c9g8s2io6i5d.eu-north-1.rds.amazonaws.com/airflow
AIRFLOW__WEBSERVER__SECRET_KEY=j38fd2h7fDd1rT2
AIRFLOW__CORE__EXECUTOR=LocalExecutor
