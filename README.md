# NYC Taxi Data Pipeline (2020–2025)

```bash
brew --prefix apache-spark
export SPARK_HOME="$(brew --prefix apache-spark)/libexec"
export PATH="$SPARK_HOME/bin:$SPARK_HOME/sbin:$PATH"
source ~/.zshrc
```
